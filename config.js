/* =========================================================
   CONFIGURACION  (opcional)
   Si pones aqui tu clave de API, la app no te la pedira.
   Si la dejas vacia, te la pide la primera vez y la guarda
   en el dispositivo.
   ========================================================= */

window.API_KEY = '';

// Tambien puedes dejar fija la carpeta, para que abra directo:
window.CARPETA = '';

window.DRIVE_API = 'https://www.googleapis.com/drive/v3';
